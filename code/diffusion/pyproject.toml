[project]
name = "diffusion"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.8",
    "torch>=2.0",
    "torchvision>=0.15",
]

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
